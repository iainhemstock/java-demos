/**
 * A String is an immutable sequence of characters. Any operation that would appear to modify the
 * string such as replace(), trim(), toLowerCase() etc actually return a new String with the
 * modification applied. The original String operated on remains unchanged.
 */
